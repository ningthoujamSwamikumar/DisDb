[package]
name = "dist-db"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.2", features = ["derive"] }
tokio = { version = "1.53.1", features = ["net", "rt-multi-thread"] }
